version: 2
updates:
  - package-ecosystem: pip
    directories:
      - /etl/extend
      - /taps/tap-exact
      - /taps/tap-hotglue
      - /targets/target-colleqtive
      - /targets/target-vendit
    schedule:
      interval: weekly
    groups:
      python-dependencies:
        patterns:
          - "*"
